// Wires @testing-library/jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.)
// into Vitest's `expect`. Loaded automatically via vitest.config.js > test.setupFiles.
import '@testing-library/jest-dom/vitest';
